import {system} from 'styled-system'

export const layout = system({
  display: true,
  height: true,
  maxHeight: true,
  maxWidth: true,
  minHeight: true,
  minWidth: true,
  overflow: true,
  overflowX: true,
  overflowY: true,
  width: true,
})
